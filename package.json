{
    "name": "incident-management",
    "devDependencies": {
        "@cap-js/sqlite": "^1",
        "@sap/cds-dk": "^7.8.2"
    },
    "scripts": {
        "cds-build": "npm install --include=dev && cds build --production"
    }
}
